apiVersion: v1
kind: Service

metadata:
  name: learn-git-branching
  labels:
    app: learn-git-branching

spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP

  selector:
    app: learn-git-branching

  type: ClusterIP